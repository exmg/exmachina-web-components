import { EditorConfiguration } from 'codemirror';

export const defaultConfiguration: EditorConfiguration = {
  mode: 'markdown',
  tabSize: 2,
  indentUnit: 2,
  indentWithTabs: true,
  lineNumbers: false,
  autofocus: false,
  lineWrapping: true,
};
